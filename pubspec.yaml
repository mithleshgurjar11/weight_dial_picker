name: weight_dial_picker
description: A customizable weight dial picker with kg/lb support
version: 0.0.4
homepage: https://github.com/yourusername/weight_dial_picker

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/preview.gif
    - assets/screenshot1.png
    - assets/screenshot2.png